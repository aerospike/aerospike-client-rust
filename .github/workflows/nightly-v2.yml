name: Aerospike Rust Client Nightly (v2)

on:
  schedule:
    - cron: '0 2 * * *'   # 02:00 UTC daily
  workflow_dispatch:
  # TEMP: validating this against v2 for the first time -- revert before merging.
  pull_request:
    branches:
      - main
      - v3

jobs:
  v2:
    name: nightly (v2)
    uses: ./.github/workflows/nightly-branch.yml
    with:
      branch: v2
    secrets: inherit
